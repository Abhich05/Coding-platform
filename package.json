{
  "scripts": {
    "setup": "npm --prefix client install && npm --prefix server install && npm --prefix server run setup:env",
    "dev": "concurrently \"npm --prefix server run start\" \"npm --prefix client run dev\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "axios": "^1.13.2",
    "mongodb": "^7.0.0",
    "zustand": "^5.0.10"
  }
}